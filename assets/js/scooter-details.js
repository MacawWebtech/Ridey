(()=>{const instrument=document.querySelector('.instrument');let played=false;const animateCluster=()=>{if(played||!instrument)return;const r=instrument.getBoundingClientRect();if(r.top<innerHeight*.72&&r.bottom>0){played=true;const needle=document.getElementById('speedNeedle'),speed=document.getElementById('speedRead'),range=document.getElementById('rangeRead'),fill=document.getElementById('rangeFill');needle.style.transform='rotate(38deg)';fill.style.width='75%';let s=0,rg=0;const timer=setInterval(()=>{s=Math.min(40,s+2);rg=Math.min(60,rg+3);speed.textContent=s;range.textContent=rg;if(s===40&&rg===60)clearInterval(timer)},45)}};addEventListener('scroll',animateCluster,{passive:true});animateCluster();document.querySelectorAll('.date-strip button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.date-strip button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');document.getElementById('dayLabel').textContent=btn.dataset.day}));if(matchMedia('(prefers-reduced-motion: reduce)').matches){document.querySelectorAll('.helmet i,.ride-dot').forEach(el=>el.style.animation='none')}})();
